## Checkpoint: 2026-04-22

This file marks a saved checkpoint of the Real School web app as of this session.

### Included in this checkpoint

- Chunk-load recovery handling and safer run/rebuild flows
- Mic permission retry without full page refresh
- Lesson top strip metronome placement and duplicate metronome cleanup
- Video rail clipping fixes
- Broad light/dark theme consistency pass (student, parent, admin, instructor, auth)
- Shared UI interaction classes for controls (`ui-input`, `ui-select`, `ui-button-*`)
- Standalone video pages hide the bottom metronome module
- Watermark overlay on playback with fullscreen persistence
- Share controls with unique share links (copy, SMS, Facebook)
- Watermark tone animation tied to playback state (runs only while playing)
- Click-on-video toggles play/pause

### Next feature queued

- Instructor-authored music notation exercises for all instruments, with playback using selected instrument timbre.
